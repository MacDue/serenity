
endpoint ResourceServer
{
    resolve(String partial_path) => (String full_path)

    add_resource_path(String resource_path) => (bool added)
    remove_resource_path(String resource_path) => (bool removed)
}
